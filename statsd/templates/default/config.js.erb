{
  port: 8125,
  debug: true,
  dumpMessages: true,
  flushInterval: 60000,
  librato: {
    email: "<%= @librato_email %>",
    token: "<%= @librato_token %>",
    source: "<%= @hostname %>",
    skipInternalMetrics: true
  },
  backends: ["statsd-librato-backend"]
}
