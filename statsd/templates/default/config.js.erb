{
  port: 8125,
  debug: true,
  dumpMessages: true,
  flushInterval: 60000,
  librato: {
    email: "<%= @librato_email %>",
    token: "<%= @librato_token %>"
  },
  backends: ["statsd-librato-backend"]
}
